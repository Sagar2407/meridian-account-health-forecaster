# Phase 0 status: repository and engineering foundation

Status: **Complete; exit gate passed on 2026-08-31**

The implementation intentionally stops before Phase 1. `make phase0-verify` was run end to end from
the user's Docker-enabled terminal and finished with `Phase 0 verification passed.`

## Deliverables

| Item | Status | Evidence |
| --- | --- | --- |
| Monorepo boundaries | PASS | Backend, frontend, config, graph, tools, retrieval, evaluation, data, tests, docs |
| Typed backend shell | PASS | FastAPI factory, validated settings, `/api/health`, API contract tests |
| Typed frontend shell | PASS | React health UI, API client, responsive styling, Vitest tests |
| Secret-safe configuration | PASS | `.env.example`, prefixed typed settings, ignored `.env*` except example |
| One-command local startup | PASS | `make docker-up` serves the UI on 5173 and the API on 8000 |
| Docker Compose health checks | PASS | Backend and frontend probes both reported healthy |
| Lint, format, type, test commands | PASS | `pyproject.toml`, frontend scripts, `Makefile`, pre-commit |
| CI definition | PASS | Separate backend and frontend GitHub Actions jobs |
| Required ADRs | PASS | LangGraph, MCP, FAISS, provider adapter, Render, persistence |
| Dependency lockfiles | PASS | `uv.lock` and `pnpm-lock.yaml` generated and consumed by frozen builds |

## Exit gate

| Criterion | Result | Evidence |
| --- | --- | --- |
| Health page loads through Docker | PASS | Both containers healthy; `/api/health` returned `status: ok` |
| No secrets in tracked source | PASS | `scripts/check_repository.py` scanned 77 files |
| No machine-specific paths in tracked source | PASS | Same scan; no user-home path or username |
| Dependency-backed lint, typing, and tests pass | PASS | Backend: ruff, ruff format, mypy strict, 4 tests at 100% coverage. Frontend: prettier, eslint, tsc, 7 tests at 95% statements / 85.71% branches |
| Resolved versions are locked | PASS | Both lockfiles generated by the same command and used by `--locked` / `--frozen-lockfile` builds |

## Defects found and fixed while running the gate

| Defect | Resolution |
| --- | --- |
| `uv` container could not create its cache in `/tmp` | Container caches moved to the ignored `.phase0-cache/` |
| Distroless `uv` image lacked a Python runtime for `uv lock` | Switched to `ghcr.io/astral-sh/uv:0.12.7-python3.12-trixie-slim` |
| `@eslint/js@^10.9.0` and `eslint@^10.9.0` do not exist | Pinned both to the ESLint 9 maintenance line, `^9.39.5` |
| Six frontend files failed `prettier --check` | Formatted with Prettier 3.9.6, the version resolved in `pnpm-lock.yaml` |
| `no-unsafe-assignment` on `import.meta.env.VITE_API_BASE_URL` | Declared `VITE_API_BASE_URL` in `frontend/src/vite-env.d.ts`, replacing the `vite/client` `any` index signature |
| Frontend branch coverage below its 80% floor | Scoped coverage to `src/**/*.{ts,tsx}` so `styles.css` is not counted as source, and covered the effect-cleanup and base-URL-fallback branches |

Phase 1 may now begin.
